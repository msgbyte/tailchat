apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: traefik-claim0
  name: traefik-claim0
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
status: {}
